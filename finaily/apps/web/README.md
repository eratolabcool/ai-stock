# Finaily Web

## Local development

```bash
cd finaily/apps/web
pnpm install
pnpm dev
```

Open:

http://localhost:3000

## Environment

Copy `.env.example` to `.env` and configure PostgreSQL when database features are enabled.
