name: Daily Finaily Intelligence

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  intelligence:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Finaily pipeline
        run: echo "Run stock update, news crawl, AI summary and SEO generation"
